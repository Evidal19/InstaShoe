{{!-- <div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      <span style="font-weight: bold">{{user.username}}</span> on {{date}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div> --}}

