{{!-- display form --}}
{{#each purchase_data as |purchase|}}
<form class="body-img" action="#">
    <div class="body-sections-img">
        <img id="pic-4" src="../images/pic 8.jpeg" alt="male black,blue and white Nikes" />
    </div>

    <div class="img-info-description">
        <p class="img-info-text">
            <span style="font-weight: bold; font-size: 20px">{{user.username}}</span><br></br>
            <span style="font-weight: bold">{{post.post_title}}</span> {{post.post_description}}<br></br>
            <span style="font-weight: bold">Amount: </span>${{purchase.purchase_amount}}
        </p>
        <button type="button" onclick="('going to comment page')" id="comment-button">comment</button>
    </div>
</form>
{{/each}}